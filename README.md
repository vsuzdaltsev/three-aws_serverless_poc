# AWS Serverless POC

## LCM


### Prerequisites

* python >= 3.8
* serverless framework
* pipenv

### Prepare environment
```
$ pipenv --python 3.9.1
$ pipenv shell
$ pipenv install invoke
```

```
$ inv -l
```